# bhai 🔊

A VS Code extension that plays a sound on every keypress while you type.

## Features

- Plays an audio clip on every keypress
- Skips audio for Enter and Backspace
- Stops previous sound and replays fresh on each new key

## Usage

Just install the extension and start typing — the sound plays automatically.

## Requirements

Windows only (uses PowerShell for audio playback).
